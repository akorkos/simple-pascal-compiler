program realtest;

var
	a, b : real; 
	step : real;
	i,j,k: integer;
	
begin
    a := 0.5;
    step := 0.5;
    b := 0.5 + 0.1;

   { if a <> b then write(a," != ",b);

    if a = b then write(a," == ",b);
}
end.